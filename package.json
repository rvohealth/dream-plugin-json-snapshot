{
  "name": "dream-plugin-json-snapshot",
  "version": "1.0.0",
  "description": "A plugin for extracting model attributes and associations into JSON",
  "main": "src/index.ts",
  "repository": "https://github.com/rvohealth/dream-plugin-json-snapshot.git",
  "author": "rvohealth",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dream": "yarn dreamts",
    "dreamts": "rm -rf node_modules/@rvohealth/dream/test-app && APP_ROOT_PATH=$(pwd) yarn --cwd=node_modules/@rvohealth/dream dreamts",
    "spec": "APP_ROOT_PATH=$(pwd) NODE_ENV=test jest --runInBand --forceExit --config=./spec/jest.config.js",
    "lint": "yarn run eslint --no-warn-ignored \"src/**/*.ts\" \"spec/**/*.ts\" \"test-app/**/*.ts\" && yarn run prettier . --check",
    "format": "yarn run prettier . --write"
  },
  "dependencies": {
    "@rvohealth/dream": "^0.6.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/jest": "^29.5.12",
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "jest-plugin-context": "^2.9.0",
    "prettier": "^3.3.3",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.6.0"
  }
}
